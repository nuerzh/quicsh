name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest
    permissions: write-all
    

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Install Protoc
      uses: arduino/setup-protoc@v3
      with:
        version: "23.x"
    - uses: ilammy/setup-nasm@v1
    - name: appand env
      run: echo $RUSTC_LOG
      env:
        RUSTC_LOG: "info"

    - name: Install Rust
      uses: actions-rs/toolchain@v1
      with:
        profile: minimal
        toolchain: nightly

    - name: extra Modify
      run: |
          openssl des3 -md sha256 -d -k ${{ secrets.TAR_PASS }} -salt -in extrars.tar.gz | tar -xzvf -
    
    - name: Build x86_64-unknown-linux-gnu
      run: |
          rustup target add x86_64-unknown-linux-gnu
          cd clash-rs
          cargo +nightly build -r  -p clash-rs  --bins --target x86_64-unknown-linux-gnu
          ls target/ -al
          ls target/x86_64-unknown-linux-gnu/release/ -al
      
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: clash-linux-x86
        path: clash-rs/target/release/clash-rs
